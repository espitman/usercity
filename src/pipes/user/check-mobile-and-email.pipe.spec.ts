import { CheckMobileAndEmailPipe } from './check-mobile-and-email.pipe';

describe('CheckMobileAndEmailPipe', () => {
  it('should be defined', () => {
    expect(new CheckMobileAndEmailPipe()).toBeDefined();
  });
});
