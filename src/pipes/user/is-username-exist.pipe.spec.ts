import { IsUsernameExistPipe } from './is-username-exist.pipe';

describe('IsUsernameExistPipe', () => {
  it('should be defined', () => {
    expect(new IsUsernameExistPipe()).toBeDefined();
  });
});
